
<div class="jumbotron row landing_page">
  <div class="col-md-3">
    <h2>Log in</h2>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="field form-group">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true %>
      </div>

      <div class="field form-group">
        <%= f.label :password %><br />
        <%= f.password_field :password, autocomplete: "off" %>
      </div>

      <% if devise_mapping.rememberable? -%>
        <div class="field check-box">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </div>
      <% end -%>

      <div class="actions">
        <%= f.submit "Log in", class: "btn btn-default btn-success" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
  <div class="col-md-4 ">
    <%= image_tag 'chiron-achilles.jpg', class: "image_landing" %>  
  </div>
  <div class="col-md-4 col-md-offset-1 login_text">
      <h4>Chiron was the greatest trainer of men in Greek mythology. Here he can be seen teaching Achilles how to shoot and arrow butt naked on a horse-man (clearly a useful skill in those days). Today training has taken on a new, more scientific spin. The Chiron system aims to be the bridge between the bleeding edge of digital fitness tracking and the hardware irl. </h4>
  </div>
</div>
