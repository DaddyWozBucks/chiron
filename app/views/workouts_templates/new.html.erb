<p>New Workout(TEMPLATE)!</p>

<%= form_for @worktemp do |f| %>

<div class="field">
	<%= f.label :name %>
	<br/>
	<%= f.text_field :name %>
</div>

<div class="actions">
<%= f.submit %>

</div>
<% end %>
